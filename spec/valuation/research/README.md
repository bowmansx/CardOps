# research/

Dated deep-dive outputs, kept whole so a claim in [[FINDINGS]] can be traced
back to what actually produced it.

Loop-written. Nothing here is edited after the fact — a superseded finding gets
a new file, not a rewrite.
