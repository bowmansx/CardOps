# What exists — the app, as it actually is

**Generated by `scripts/app-map.mjs`. Do not edit — regenerate.**

Written because "does this already exist?" kept having a surprising answer.
Five times in two days a request turned out to be for something already built.
A hand-written list would go stale and then lie; this one walks the repo, so
what it says exists, exists.

*Last generated from the working tree. Counts: 55 tables, 36 pages, 53 API routes, 45 card modules.*

---

## Tables the app code never names

**Read this section first** — it is the cheapest place to look before building
something new. Each of these exists in the database and no TypeScript in
`src/` mentions it.

That does NOT mean dead. A table written entirely by a SQL function is correct
design, not an oversight — `purchase_lot_adjustments` is appended by
`card_sell` and `card_unsell`, and the app never touching it directly is the
point. So the SQL mention count is shown rather than a verdict: a low count
usually means nothing uses it, a high one usually means SQL does. **Usually —
check before concluding.** Tenancy-hardening loops list every table name in an
array, which inflates the count for tables that are genuinely dead.

| Table | Mentions in migrations |
|---|---|
| `invite_codes` | 2 |
| `card_valuations` | 3 |
| `card_import_staging` | 3 |
| `card_intake_items` | 3 |
| `card_watchlist` | 3 |
| `card_flag_events` | 3 |
| `card_grading_submissions` | 3 |
| `card_import_batches` | 4 |
| `card_documents` | 8 |
| `purchase_lot_adjustments` | 13 |
| `card_pool_adjustments` | 25 |

---

## Screens

- `/`
- `/auth/callback`
- `/cards`
- `/cards/[id]`
- `/cards/[id]/edit`
- `/cards/[id]/label`
- `/cards/[id]/sell`
- `/cards/[id]/value`
- `/cards/books`
- `/cards/books/push-preview`
- `/cards/books/simulator`
- `/cards/bulk`
- `/cards/businesses`
- `/cards/businesses/[id]/connect`
- `/cards/credits`
- `/cards/ebay`
- `/cards/export`
- `/cards/find`
- `/cards/import`
- `/cards/intake`
- `/cards/lots`
- `/cards/movers`
- `/cards/new`
- `/cards/news`
- `/cards/portfolio`
- `/cards/pricing`
- `/cards/receipts`
- `/cards/reports`
- `/cards/sales`
- `/cards/services`
- `/cards/settings`
- `/cards/show`
- `/cards/showcases`
- `/cards/watchlist`
- `/login`
- `/showcase/[token]`

## API routes

- `/api/cards/alerts`
- `/api/cards/basis-items`
- `/api/cards/books/journal`
- `/api/cards/books/post`
- `/api/cards/bulk`
- `/api/cards/businesses`
- `/api/cards/comps/import`
- `/api/cards/connectors`
- `/api/cards/connectors/push`
- `/api/cards/daemon`
- `/api/cards/describe`
- `/api/cards/estimate`
- `/api/cards/export`
- `/api/cards/find`
- `/api/cards/grade-estimate`
- `/api/cards/grade-ev`
- `/api/cards/groups`
- `/api/cards/intake/scan`
- `/api/cards/intel`
- `/api/cards/lots`
- `/api/cards/movers`
- `/api/cards/photo-presets`
- `/api/cards/photo-templates`
- `/api/cards/prefs`
- `/api/cards/price-sources/refresh`
- `/api/cards/pricing/generate`
- `/api/cards/receipts`
- `/api/cards/receipts/scan`
- `/api/cards/reports`
- `/api/cards/settings`
- `/api/cards/showcases`
- `/api/cards/unsell`
- `/api/cron/card-alerts`
- `/api/cron/card-estimates`
- `/api/cron/card-news`
- `/api/cron/price-refresh`
- `/api/ebay/account-deletion`
- `/api/ebay/callback`
- `/api/ebay/cancel-order`
- `/api/ebay/connect`
- `/api/ebay/end`
- `/api/ebay/feedback`
- `/api/ebay/hub`
- `/api/ebay/list`
- `/api/ebay/list-lot`
- `/api/ebay/location`
- `/api/ebay/messages`
- `/api/ebay/offers`
- `/api/ebay/relist`
- `/api/ebay/revise`
- `/api/ebay/send-offer`
- `/api/ebay/ship`
- `/api/ebay/sync`

---

## The card modules, and what each is for

Each summary is the module's own first comment line — so if it reads badly,
the fix is in the file, not here.

### `basis`

Basis helpers (foundation-fixes item 3; cost lines added 2026-07-25).

Does: `lotAverages`, `lotRemainingTotal`, `cardAcquisitionBasis`, `cardCostLines`, `cardBasis`

### `blend`

Consensus blend (pure, no I/O — safe to import on the client). Beau wanted a

Does: `consensusForCard`

### `camera`

Camera math (Beau, 2026-07-25). Pure, unit-tested, no DOM — CameraSheet does

Does: `withMargin`, `sharpness`, `frameDelta`, `shouldAutoSnap`, `pickSharpest`

### `card-intel-schema`

Card Intel: live-web news + a disciplined verdict + sell-timing strategy.

Does: `parseStoredIntel`

### `credits`

Credit metering for pricing intelligence (Beau, 2026-07-22). Pure cost model:

Does: `estimateCost`, `bandFor`, `costFill`, `normalizeEstimate`

### `csv`

CSV connector (Beau, 2026-07-24). Always available, needs no credentials and no

Does: `entriesToCsv`

### `digest`

Top-movers digest assembly (Beau, 2026-07-24). Pure: takes a user's cards and

Does: `cardLabel`, `currentPrice`, `buildMoversDigest`

### `edges`

Card edge detection (Beau, 2026-07-26). Pure, unit-tested, no DOM —

Does: `releaseScratch`, `sobel`, `houghLines`, `subBin`, `footPoint`, `alignTo`, `angleGap`, `intersect`, `quadFromLines`, `segmentSupport`, `orderQuad`, `validQuad`, `polygonArea`, `isConvex`, `frameFill`, `clipToRect`, `focalPx`, `tiltDegrees`, `distanceInches`, `apparentAspect`, `guessSize`, `detectCard`

### `estimate`

Pure helpers for the estimate engine (Beau, 2026-07-22). Building blocks the

Does: `compsAsSales`, `groundPrice`, `medianOf`, `summarizeSales`, `comparableQueries`, `buildEstimateDigest`

### `estimate-run`

The estimate engine, extracted so ONE code path serves both the on-demand

Does: `runEstimate`

### `evidence`

Evidence integrity + backup state (Beau, 2026-07-25). Wave B §5.

Does: `sha256Hex`, `verifiedCopy`, `evidenceHealth`, `missingEvidence`

### `export`

CardOps translation layer (contract §6): profile-driven CSV export. A

Does: `computeField`, `buildCsv`

### `exposure`

Is the glare in this frame something a sweep could ever remove? (2026-07-27)

Does: `clipFraction`, `globalMedian`, `readGlare`, `glareAdvice`

### `grade-estimate-schema`

Shared shape for the AI grade estimate — the route validates output with it

Does: `parseStoredEstimate`

### `grading-ev`

Is it worth grading this card? (2026-07-29)

Does: `gradeDistribution`, `gradingVerdict`, `verdictLine`

### `grading-prompt`

AUTO-EXTRACTED from reference/grading-rubric.md (deep-researched, adversarially

_(no exported functions)_

### `img`

Client-only: downscale a captured photo to a JPEG data URL (max edge px).

Does: `downscale`

### `index`

The connector registry. Adding QuickBooks is one file dropped in here — the

Does: `getConnector`, `connectorOptions`

### `index`

The registry, in display order. Add a new vendor by dropping an adapter file

Does: `runnableAdapters`, `sourceAvailability`

### `intake-session`

The cards booked in THIS sitting — what you just did, still on screen.

Does: `addToSession`, `removeFromSession`, `sessionLabel`, `sessionSummary`, `sessionPace`

### `ledger`

Backend-neutral ledger translation (Beau, 2026-07-24). Groups CardOps' internal

Does: `buildPushEntries`

### `liquidity`

CardOps liquidity (Beau, 2026-07-25) — pure functions, no I/O, server+client

Does: `velocity`, `tierOf`, `weightedPrices`, `shareAtOrAbove`, `sellEstimate`, `formatEta`, `matchesExact`

### `market-sales`

Observed-sales history (Beau, 2026-07-23). Turns the Card API's raw sales into

Does: `saleKey`, `saleToRow`, `salesToRows`, `storedToSales`, `dailyMedianSeries`

### `match`

FIND: "which card in MY inventory is this?" (Beau, 2026-07-26)

Does: `norm`, `normNumber`, `setWords`, `setsAgree`, `namesAgree`, `scoreCard`, `confidenceOf`, `findMatches`

### `movers`

Card price movement (Beau, 2026-07-20). Pure, testable math over a card's

Does: `pctChangeOverWindow`, `classifyMove`, `trendDeviation`

### `news`

Card news (Beau, 2026-07-20). Pure helpers for the news pipeline: derive the

Does: `subjectsFromCards`, `googleNewsRssUrl`, `parseRssItems`

### `photo-prefs`

Photo capture preferences (Beau, 2026-07-25) — CAPTURE_WORK_ITEMS.md P2.

Does: `normalizePhotoPrefs`, `estimateBytesPerCard`, `humanBytes`

### `photo-set`

Which photos REPRESENT a card, and in what order (Beau, 2026-07-25) —

Does: `displayPhotos`, `listingPhotos`, `gradingPhotos`, `roleCaption`

### `pricecharting`

PriceCharting adapter — Beau's pick. Broad coverage (TCG + games + some

_(no exported functions)_

### `roles`

Server-only role resolution. RLS is the real boundary; these gate the UI.

Does: `currentRole`, `hasCardAccess`

### `scryfall`

Scryfall adapter — MTG only, FREE and no key (Beau's Tarkir: Dragonstorm card

_(no exported functions)_

### `session`

The photo SESSION — the shot list, what has been captured into each slot,

Does: `sessionToShots`, `existingSlots`, `nextOpen`, `reorder`

### `settings`

CardOps owner preferences — stored in user_settings.prefs.cardops (jsonb,

Does: `cardOpsPrefs`

### `sku`

SKU: {CAT}-{YYYY}-{seq}  e.g. FB-2026-000412 (contract §3).

Does: `catCode`, `buildSku`

### `skudb`

Server-only: next sequential SKU for a category+year (read-max +1).

Does: `nextSku`

### `templates`

Photo templates (Beau, 2026-07-25) — CAPTURE_WORK_ITEMS.md P3.

Does: `normalizeShots`, `missingShots`, `shotStep`, `guideToTarget`

### `thecardapi`

The Card API adapter (Beau, 2026-07-21) — realized SOLD sales aggregated across

Does: `saleQuery`, `distillSales`, `fetchCardApiSales`, `fetchCardApiByQuery`

### `types`

CardOps accounting connectors (Beau, 2026-07-24). CardOps keeps its OWN books;

_(no exported functions)_

### `types`

Multi-source pricing (Beau, 2026-07-20). A "source quote" is one vendor's

_(no exported functions)_

### `types`

CardOps shared types + option vocab (client-safe).

Does: `categoryKind`, `deriveTags`

### `upload`

Direct-to-storage photo upload (Beau, 2026-07-25).

Does: `uploadCardPhotos`

### `use-photo-prefs`

One fetch per page load, shared by every capture surface. Speed Book, batch

Does: `resetPhotoPrefsCache`, `usePhotoPrefs`

### `valuation`

CardOps valuation (contract §5) — pure functions, no I/O, server+client safe.

Does: `eraOf`, `rawValue`, `applyStrategy`, `marketValue`, `adjustToGrade`, `interpretPipeline`, `valueAt`, `computeMarketValue`, `suggestedListPrice`, `buildLadder`, `gradeUp`

### `zoho`

Zoho Books connector (Beau, 2026-07-24). Formats CardOps' neutral push entries

Does: `toZohoJournal`

### `zpl`

ZPL for a 1.5"W x 1"L label on a Zebra ZD411 (203 dpi → 305 x 203 dots).

Does: `buildLabelZpl`


---

## Every table

- **`audit_log`** — id, ts, actor, action, target, payload, result
- **`card_account_map`** — id, business_id, provider, account_key, external_account_id, external_account_name, updated_at
- **`card_alerts`** — card_id, target_price, direction, note, created_at
- **`card_basis_item_kinds`** — id, user_id, key, label, sort, archived, created_at
- **`card_basis_items`** — id, card_id, user_id, kind_key, label, amount, incurred_on, note, created_at, updated_at
- **`card_businesses`** — id, user_id, name, short_code, type, zoho_books_org_id, active, created_at
- **`card_comps`** — id, fingerprint, card_id, source, grader, sale_price, listing_url, fetched_at
- **`card_documents`** — id, card_id, user_id, proves, kind, bucket, path, doc_date, bytes, sha256, backup_state, backup_error, backed_up_at, created_at
- **`card_estimates`** — id, card_id, mode, value, low, high, confidence, rationale, sources, credits_spent, model, created_by, created_at
- **`card_flag_events`** — id, card_id, flag, payload
- **`card_format_profiles`** — id, name, direction, file_type, header_hash, column_order, transforms, constants, validation, learned_from_import, active, created_at
- **`card_grade_multipliers`** — id, grader, category, multiplier, source, updated_at, unique
- **`card_grading_submissions`** — id, card_id, grader, expected_grade, roi_predicted
- **`card_group_items`** — group_id, card_id, added_at, primary
- **`card_groups`** — id, name, color, sort, created_at
- **`card_identities`** — id, fingerprint, sport_category, year, set_name, player, card_number, parallel, last_refreshed_at, created_at
- **`card_import_batches`** — id, profile_id, source_filename, status, created_at
- **`card_import_staging`** — id, batch_id, raw, match_kind, matched_card_id, resolution, committed
- **`card_intake_items`** — id, session_id, photos, vision_raw, status, card_id, created_at
- **`card_intake_sessions`** — id, mode, started_at
- **`card_lot_items`** — lot_id, card_id, comp_value_at_add, primary
- **`card_lots`** — id, sku, title, description, status, ask_price, listing_refs, created_at, updated_at
- **`card_market_sales`** — id, card_id, source, external_id, title, price, currency, grader, grade, platform, sold_at, raw, seen_at
- **`card_news`** — id, subject, card_id, title, url, source, published_at, significance, direction, market_moving, summary, notified, created_at, unique
- **`card_photo_presets`** — id, user_id, name, settings, created_at, unique
- **`card_photos`** — id, card_id, kind, variant, storage_provider, bucket, width, created_at
- **`card_pool`** — id, entity_id, name, total_cost, card_count
- **`card_pool_adjustments`** — id, pool_id, ts, kind, card_id, amount, total_after, actor
- **`card_portfolio_snapshots`** — snapshot_date, card_count, cost_basis, market_value, created_at, updated_at
- **`card_price_history`** — id, card_id, price, ts
- **`card_pricing_strategies`** — key, label, target_rule, window_days, min_comps, outlier_rule, params, notes
- **`card_push_log`** — id, business_id, provider, reference, external_id, pushed_at, pushed_by
- **`card_receipts`** — id, entity_id, receipt_date, vendor, amount, image_path, note, disposition, card_ids, to_entity_id, advance_disposition, posted, created_at, updated_at
- **`card_sales`** — id, card_id, platform, sale_price, shipping_income, net_proceeds, order_ref, synced_to_books
- **`card_showcases`** — id, user_id, token, title, card_ids, show_prices, for_sale, is_public, contact, created_at, updated_at
- **`card_source_quotes`** — id, card_id, source, kind, grader, grade, price, currency, label, url, product_ref, payload, fetched_at, slot
- **`card_storage_locations`** — name, created_at
- **`card_user_prefs`** — user_id, auto_estimate, estimate_model, updated_at
- **`card_valuations`** — id, card_id, grader, grade, value, basis_source, confidence, as_of, unique
- **`card_watchlist`** — id, player
- **`cards`** — id, sku, barcode, entity_id, player, parallel, sport_category, team, is_relic, condition_type, raw_grade_estimate, grader, qualifiers, acquired_date … +20 more
- **`credit_ledger`** — id, user_id, delta, reason, ref, created_at
- **`ebay_cancelled_orders`** — order_ref, cancelled_at
- **`ebay_connections`** — user_id, access_token, refresh_token, token_expiry, scopes, ebay_user, updated_at
- **`entities`** — id, name, short_code, type, zoho_books_org_id, active, created_at
- **`invite_codes`** — code, created_by, uses_left, expires_at, created_at
- **`journal_entries`** — id, entity_id, entry_date, source, source_ref, line, account, debit, credit, memo, created_at
- **`profiles`** — id, full_name, created_at
- **`purchase_lot_adjustments`** — id, lot_id, ts, kind, card_id, amount, remaining_cost_after, remaining_count_after, actor, note
- **`purchase_lots`** — id, user_id, label, source, acquired_on, tax_bucket, total_cost, card_count, remaining_cost, remaining_count, receipt_id, created_at
- **`push_subscriptions`** — id, endpoint, keys, user_agent, created_at
- **`service_config`** — key, enabled, mode, last_verified
- **`todos`** — id, title, notes, bucket, entity, due_date, gcal_event_id, rollover_flag, done_at, last_touched_at, created_at
- **`user_settings`** — user_id, calendar_days, day_start, day_end, masterops_calendar_id, updated_at
- **`user_storage_usage`** — user_id, bytes, objects, updated_at
