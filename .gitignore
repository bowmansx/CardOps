node_modules
.next
.env*.local
.vercel
*.tsbuildinfo
next-env.d.ts

# Obsidian workspace state — the notes are tracked, the UI state is not
spec/.obsidian/workspace*.json
spec/.obsidian/cache
spec/.trash/

# Local backups — real data, never committed
backups/
